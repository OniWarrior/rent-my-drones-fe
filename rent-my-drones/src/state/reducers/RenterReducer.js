import {
    RENTED_START,
    RENTED_SUCCESS,
    RENTED_FAILURE
} from '../actions/RentedActions'


const initialState={
    rentedDrones:{},
    loading:true,
    error:''
}

const rentedReducer=(state=initialState,action)=>{

}